name: Workflow with Email Identifier

on:
  # Runs when manually triggered
  workflow_dispatch:
  # Also runs on push to main branch
  push:
    branches: [ main ]

jobs:
  hello-world:
    runs-on: ubuntu-latest
    
    steps:
      - name: 22f2001725[at]ds[dot]study[dot]iitm[dot]ac[dot]in
        run: echo "Hello, world!"

        
      - name: Display date
        run: date
